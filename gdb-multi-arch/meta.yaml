{% set name = "multi-arch-gdb" %}
{% set version = "17.2" %}
{% set sha256 = "e26244bbf766af83440bc993f7e738368b55b2a3ff45a68d83f761c0f0cd6fc5" %}

package:
  name: {{ name }}
  version: {{ version }}.memfault1

source:
  - url: https://github.com/memfault/binutils-gdb/archive/gdb-{{ version }}-release.tar.gz
    sha256: {{ sha256 }}

build:
  number: '1'
  skip: True  # [win or (osx and x86_64)]

requirements:
  build:
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}  # [linux]
    - {{ compiler('cxx') }}  # [linux]
    - make
    - bison # [linux]
  host:
    - python {{ python }}
    - texinfo
    - ncurses {{ ncurses }}
    - xz {{ xz }}
    - zlib {{ zlib }}
    - zstd {{ zstd }}
    - libiconv {{ libiconv }}
    - mpfr {{ mpfr }}
    - gmp {{ gmp }}
    - expat {{ expat }} # [linux]
    # For libdebuginfod:
    - elfutils
  run:
    - python {{ python }}
    - ncurses {{ ncurses }}
    - xz {{ xz }}
    - zlib {{ zlib }}
    - libiconv {{ libiconv }}
    - mpfr {{ mpfr }}
    - gmp {{ gmp }}
    - expat {{ expat }} # [linux]
    # For libdebuginfod:
    - elfutils

test:
  commands:
    - gdb -ex "print 1" --batch

about:
  home: https://www.gnu.org/software/gdb/
  license: GPL-2.0
  license_family: GPL
  license_file: COPYING
  summary: GDB, the GNU Project debugger, allows you to see what is going on inside another program while it executes -- or what another program was doing at the moment it crashed.
  description: |
    GDB, the GNU Project debugger, allows you to see what is going on `inside'
    another program while it executes -- or what another program was doing at
    the moment it crashed.
    The program being debugged can be written in Ada, C, C++, Objective-C,
    Pascal (and many other languages). Those programs might be executing on the
    same machine as GDB (native) or on another machine (remote).
    GDB can run on most popular UNIX and Microsoft Windows variants.
  doc_url: https://sourceware.org/gdb/current/onlinedocs/gdb/
  dev_url: git://sourceware.org/git/binutils-gdb.git

extra:
  recipe-maintainers:
    - gqmelo
    - marcelotrevisani
